# ---- Web framework ----
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
python-dotenv==1.0.1
requests==2.32.3

# ---- ML / Computer Vision (inference side) ----
# NOTE: install the CUDA build of torch separately for your GTX 1650.
# See README "GPU setup". ultralytics pulls torch automatically but the
# CPU wheel — we override it during setup.
# Stage 1: YOLO26-X-SEG instance segmentation (yolo26x-seg.pt). YOLO26 support
# requires ultralytics >= 8.4 — do NOT downgrade to the old 8.3.0 pin.
ultralytics==8.4.90
# NOTE: the Ultralytics CLIP fork (git+https://github.com/ultralytics/CLIP.git)
# is NO LONGER required — it only served YOLO-World's set_classes() in the
# archived pivots. Harmless if already installed.
# Stage 2 material classifier: supervised ViT fine-tuned on TrashNet-enhanced
# (edwinpalegre/ee8225-group4-vit-trashnet-enhanced, ~343 MB to the HF cache
# on first load).
transformers==4.44.2
opencv-python-headless==4.10.0.84
pillow==10.4.0
numpy==1.26.4

# ---- Validation ----
pydantic==2.9.2

# ---- Testing ----
pytest==8.3.3
pytest-mock==3.14.0

# ---- Production server (deployment) ----
gunicorn==23.0.0
